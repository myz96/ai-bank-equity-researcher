The code has **not stabilised**: five new correctness issues remain.

1. **High — fatal claim-specific checks still bypass the driver-confidence cap.** [validate.py:1312](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/validate.py:1312) recognizes only `drivers_reconcile` and `movement_level_not_ratio_sized`. Fatal `comparison_leak` and `component_from_prior_half` failures identify a demonstrably wrong driver, yet `cap_unreconciled_drivers` leaves it at 95. Reproduced for both failure strings. This preserves the original calibration hole on wrong-comparison and wrong-column paths.

2. **High — model-supplied NumberFact units are still trusted.** [validate.py:1108](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/validate.py:1108) accepts a fact whenever its raw magnitude matches before checking unit conversion. Consequently, `quote_prints("NPAT was $150m", 150, "bps")` is `True`; `_mint_record` retains the invented `150 bps` fact, and the weak-citation cap lets a `+150 bps` driver remain at 95. The open-loop extractor also accepts NumberFacts without validating them against their quote at [extract.py:182](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/extract.py:182).

3. **Medium-high — narrowed marker stripping still removes semantic identifiers.** [research_agent.py:733](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/research_agent.py:733) cannot distinguish a footnote from a label-ending digit before a value. `match_quote("Peer 157", "Peer 1 157")` and the equivalent `Level 2 298` case both pass under marker relaxation. The pattern matches 1,353 spans in the current 30-document corpus, including meaningful rows such as `stage 2 4,504`.

4. **Medium — the cost ceiling is not enforced between a chart’s two vision calls.** The budget check occurs once before dispatch at [research_agent.py:1631](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/research_agent.py:1631), but `read_chart` then performs the walk read and annotation read at [research_agent.py:903](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/research_agent.py:903) and [research_agent.py:958](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/research_agent.py:958) without rechecking cost. Reproduced: a $0.50 ceiling allowed two $0.60 vision calls, ending at $1.20.

5. **Medium — the unit-typed reconciliation still adds an off-unit residual.** [validate.py:1212](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/validate.py:1212) flags the residual as mismatched, but [validate.py:1225](/Users/michaelzhao/swe/ai-bank-equity-researcher/src/bank_equity_researcher/validate.py:1225) unconditionally adds its value. A `$m` movement with `+310 $m` drivers and a `+3 bps` residual therefore returns both `drivers_reconcile` **PASS** and `drivers_unit_mismatch` **FAIL**. `sign_flip_hint` repeats the same unit-blind residual arithmetic.
219,960
