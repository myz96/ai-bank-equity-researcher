# 17 — Build the gold set

Type: task
Status: claimed
Blocked by: 18

## Question

Build the ~42-case gold set per ticket 05: hand-recorded walks (NIM, cash earnings, CET1), deterministic level-1 arithmetic plus documented driver lists (ROE, CTI), note splits (impairment), and the four named adversarial cases. Every gold value carries provenance (document, page, verification method); every gold walk passes the sum checks; the set splits ~34 dev / 8 holdout. HITL: the user spot-checks a sample before the matrix runs. CBA cases can start before ticket 18 completes.
